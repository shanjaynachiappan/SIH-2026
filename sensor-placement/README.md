# Part A — Sensor Placement Pipeline (NCB/PFM + turf.js)

Every script has been run end-to-end in this environment (real output counts
shown below) — this is a tested pipeline, not just written code.

## Project structure
```
sensor-placement/
├── backend/
│   ├── ncb_pfm.py           # Step 2: physics risk grid (NCB + Profile Function Method)
│   └── requirements.txt
├── placement/
│   ├── package.json         # @turf/turf dependency
│   ├── idw_interpolate.js   # Step 3: IDW on historical subsidence points
│   ├── buffer_zone.js       # Step 4: angle-of-draw buffer + consequence weighting
│   ├── composite_risk.js    # Step 5: weighted composite risk score
│   ├── zone_polygons.js     # Step 6: risk zone polygons (concave hull)
│   ├── node_placement.js    # Step 7: candidate grid -> tiered node placement
│   └── run_pipeline.sh      # runs all steps in order
└── data/
    ├── panel_boundary.geojson              (sample input)
    ├── historical_subsidence_points.geojson (sample input)
    ├── surface_assets.geojson               (sample input)
    └── *.geojson  (generated by running the pipeline)
```

## Steps to run

```bash
cd sensor-placement/backend
pip install -r requirements.txt

cd ../placement
npm install
bash run_pipeline.sh
```

This runs, in order: NCB/PFM grid → IDW interpolation → buffer + consequence
weighting → composite risk scoring → risk zone polygons → node placement.

Final output: `data/optimal_node_positions.geojson` — a GeoJSON
FeatureCollection of node positions with `node_tier` (Full/Lite/Crack)
assigned per point. Load this directly into React/Leaflet — no conversion
needed, turf.js and Leaflet both speak GeoJSON natively.

## Actual results from this run
- NCB/PFM grid: 1,681 points (panel W=150m, H=100m, m=2.5m, W/H=1.5)
- IDW: 9 interpolated points from 8 sample historical measurements
- Risk zones: High=500 pts, Medium=434 pts, Low=747 pts (concave hull polygons)
- Final placement: 114 nodes from 1,681 candidates (**Full=32, Crack=36, Lite=46**)

## Bug fixed — Full vs Crack tier split
Earlier version used `Math.abs(strain_ue) > threshold` to decide Crack vs
Full within the High-risk zone. This was physically wrong: **only tensile
(positive) strain causes cracking** — compressive strain (negative, near the
trough center, where subsidence is highest) does not, and that's exactly
where Full nodes belong (near the retreat line, subsidence-dominant zone).
Using the absolute value routed those high-compressive-strain points into
"Crack" instead of "Full", leaving 0 Full nodes.

**Fix**: `node_placement.js` now uses **signed** strain
(`strain_ue > TENSILE_STRAIN_THRESHOLD_UE`, no `Math.abs`) so only genuinely
tensile points become Crack nodes; high-risk compressive points correctly
become Full nodes. `TENSILE_STRAIN_THRESHOLD_UE` (150 µε by default) is
still tunable — inspect `composite_risk_classified.geojson`'s strain range
for your actual panel and adjust if the split still looks off.

## Sample data note
`historical_subsidence_points.geojson`, `surface_assets.geojson`, and
`panel_boundary.geojson` are placeholder coordinates (Jharia coalfield area)
for testing — replace with your actual mine's real panel geometry, survey
points, and asset locations before using for the real submission.

## CMRI formula upgrade (replaces the earlier NCB/tanh-PFM placeholder)
`backend/ncb_pfm.py` now implements the **verified CMRI formula** (Kumar,
Singh & Sinha, 1973), not a generic UK NCB/tanh approximation:
```
Sm = t * a * cos(d)          a = 0.5 * (0.9 + P)
Y(x) = Sm * (1 - x^2/l^2)^2   I(x) = 4*(Sm/l)*(x/l - (x/l)^3)
```
`P` (Rock Factor) comes from `backend/rock_factor.py`, weighted by stratum
hardness (hard/medium/soft Q coefficients) — pass real borehole-log strata
via the `panel_boundary.geojson`'s `strata` property, or it falls back to a
representative "medium hard" default. `ncb_pfm.py` now reads panel geometry
**from `panel_boundary.geojson` dynamically** instead of hardcoded constants,
so the same script works for any real panel without code changes.

## Confidence scoring (new pipeline step)
`placement/confidence_score.js` runs between `composite_risk.js` and
`zone_polygons.js`, tagging every point with a `confidence_tier`
(Low/Medium/High) from four inputs: W/H extremity, local historical-data
point density, unmodeled-factor flags (water/fault/old-workings — set via
`panel_boundary.geojson` properties), and optional regional benchmark
deviation. `node_placement.js` uses this to **halve minimum spacing in
Low-confidence zones**, placing denser fallback coverage exactly where the
theoretical prediction is least trustworthy.

## Live API — connects both dashboards to this pipeline
`api_server.py` (FastAPI) wraps the whole pipeline behind HTTP so the
**central dashboard's "Generate Placement" button** and the **local
dashboard's Node Placement page** both read the same real output — no more
hardcoded/random node generation on either side.

### Run it
```bash
cd sensor-placement
pip install -r requirements-api.txt
python api_server.py            # serves on http://localhost:8001
```
Then start both frontends as usual (`npm run dev` in `Frontend/` and
`frontend-2/`) — both already point at `VITE_SENSOR_PLACEMENT_API_URL`
(`http://localhost:8001/api` by default, set in each app's `.env`).

**Flow**: central dashboard's Sensor Placement page → "Generate Placement" →
`POST /api/node-placement/run` (writes the real panel polygon, runs the full
CMRI → IDW → buffer → composite risk → confidence → zone polygons → node
placement chain via the existing tested scripts, caches the result) → same
GeoJSON is what the local dashboard's Node Placement page reads via
`GET /api/node-placement` — same map, same data, two views.

If the API is unreachable, the central dashboard falls back to the old
random generator (logged to console) so the UI still demos — this is a
fallback path only, not the intended behavior.